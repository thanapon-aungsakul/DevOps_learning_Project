on:
 push:
  branches:
   - main
Jobs:
 build:
  runs-on: ubuntu-latest
  steps:
   - run: echo "Building..."
 test:
  runs-on: ubuntu-latest
  needs: build
  steps:
   - run: echo "Testung..."
 hello:
  runs-on: ubuntu-latest
  steps:
   - run: echo"Hello World"  
